'use strict';

import React from 'react';

export default class Error extends React.Component {
    render() {
        return (
            <nav className="Error">Sorry, no such city..</nav>
        )
    }
}